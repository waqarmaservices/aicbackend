apiVersion: v1
kind: Service
metadata:
  name: {{ include "aic-backend-stage.fullname" . }}
  labels:
    {{- include "aic-backend-stage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aic-backend-stage.selectorLabels" . | nindent 4 }}